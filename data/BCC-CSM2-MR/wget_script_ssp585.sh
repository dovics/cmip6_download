#!/bin/bash
##############################################################################
# ESG Federation download script
#
# Template version: 1.2
# Generated by localhost - 2024/10/27 20:30:57
# Search URL: http://localhost:8080/esg-search/wget?dataset_id=CMIP6.ScenarioMIP.BCC.BCC-CSM2-MR.ssp585.r1i1p1f1.day.hur.gn.v20190318|aims3.llnl.gov,CMIP6.ScenarioMIP.BCC.BCC-CSM2-MR.ssp585.r1i1p1f1.day.pr.gn.v20190318|aims3.llnl.gov,CMIP6.ScenarioMIP.BCC.BCC-CSM2-MR.ssp585.r1i1p1f1.day.rsds.gn.v20190318|aims3.llnl.gov,CMIP6.ScenarioMIP.BCC.BCC-CSM2-MR.ssp585.r1i1p1f1.day.sfcWind.gn.v20190318|aims3.llnl.gov,CMIP6.ScenarioMIP.BCC.BCC-CSM2-MR.ssp585.r1i1p1f1.day.tasmin.gn.v20190318|aims3.llnl.gov,CMIP6.ScenarioMIP.BCC.BCC-CSM2-MR.ssp585.r1i1p1f1.day.tasmax.gn.v20190318|aims3.llnl.gov,CMIP6.ScenarioMIP.BCC.BCC-CSM2-MR.ssp585.r1i1p1f1.day.tas.gn.v20190318|aims3.llnl.gov
#
###############################################################################
# first be sure it's bash... anything out of bash or sh will break
# and the test will assure we are not using sh instead of bash
if [ $BASH ] && [ `basename $BASH` != bash ]; then
    echo "######## This is a bash script! ##############" 
    echo "Change the execution bit 'chmod u+x $0' or start with 'bash $0' instead of sh."
    echo "Trying to recover automatically..."
    sleep 1
    /bin/bash $0 $@
    exit $?
fi

version=1.3.2
CACHE_FILE=.$(basename $0).status
openId=
search_url='http://localhost:8080/esg-search/wget?dataset_id=CMIP6.ScenarioMIP.BCC.BCC-CSM2-MR.ssp585.r1i1p1f1.day.hur.gn.v20190318|aims3.llnl.gov,CMIP6.ScenarioMIP.BCC.BCC-CSM2-MR.ssp585.r1i1p1f1.day.pr.gn.v20190318|aims3.llnl.gov,CMIP6.ScenarioMIP.BCC.BCC-CSM2-MR.ssp585.r1i1p1f1.day.rsds.gn.v20190318|aims3.llnl.gov,CMIP6.ScenarioMIP.BCC.BCC-CSM2-MR.ssp585.r1i1p1f1.day.sfcWind.gn.v20190318|aims3.llnl.gov,CMIP6.ScenarioMIP.BCC.BCC-CSM2-MR.ssp585.r1i1p1f1.day.tasmin.gn.v20190318|aims3.llnl.gov,CMIP6.ScenarioMIP.BCC.BCC-CSM2-MR.ssp585.r1i1p1f1.day.tasmax.gn.v20190318|aims3.llnl.gov,CMIP6.ScenarioMIP.BCC.BCC-CSM2-MR.ssp585.r1i1p1f1.day.tas.gn.v20190318|aims3.llnl.gov'

#These are the embedded files to be downloaded
download_files="$(cat <<EOF--dataset.file.url.chksum_type.chksum
'hur_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20150101-20171231.nc' 'http://aims3.llnl.gov/thredds/fileServer/css03_data/CMIP6/ScenarioMIP/BCC/BCC-CSM2-MR/ssp585/r1i1p1f1/day/hur/gn/v20190318/hur_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20150101-20171231.nc' 'SHA256' '1c4d09637c48a802ea2a8f908e431a9742ca24f7f07bc7da0099ae4d372327ff'
'hur_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20180101-20201231.nc' 'http://aims3.llnl.gov/thredds/fileServer/css03_data/CMIP6/ScenarioMIP/BCC/BCC-CSM2-MR/ssp585/r1i1p1f1/day/hur/gn/v20190318/hur_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20180101-20201231.nc' 'SHA256' '744fd42e017d3704688b5ba2b3dbb51d1c245a6a0ce7cc9602a50dc1f9e6109f'
'hur_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20210101-20231231.nc' 'http://aims3.llnl.gov/thredds/fileServer/css03_data/CMIP6/ScenarioMIP/BCC/BCC-CSM2-MR/ssp585/r1i1p1f1/day/hur/gn/v20190318/hur_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20210101-20231231.nc' 'SHA256' '5d4ed602d332e7300bce52a187126a9d6a4681ee52de093d60f3a318526c5714'
'hur_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20240101-20261231.nc' 'http://aims3.llnl.gov/thredds/fileServer/css03_data/CMIP6/ScenarioMIP/BCC/BCC-CSM2-MR/ssp585/r1i1p1f1/day/hur/gn/v20190318/hur_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20240101-20261231.nc' 'SHA256' '19642f1a138dc4ab37f1296ca6cdce3bd04425ec28338cf7566bc5b4860a3c1f'
'hur_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20270101-20291231.nc' 'http://aims3.llnl.gov/thredds/fileServer/css03_data/CMIP6/ScenarioMIP/BCC/BCC-CSM2-MR/ssp585/r1i1p1f1/day/hur/gn/v20190318/hur_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20270101-20291231.nc' 'SHA256' '38daa212da188c00344c33f81099867e10b9e1d11b759b92679792d0a9a2cc46'
'hur_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20300101-20321231.nc' 'http://aims3.llnl.gov/thredds/fileServer/css03_data/CMIP6/ScenarioMIP/BCC/BCC-CSM2-MR/ssp585/r1i1p1f1/day/hur/gn/v20190318/hur_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20300101-20321231.nc' 'SHA256' 'b6a1384a9bdda50b5631f69879a10089ad5574660c4e5ffb5fa8f7acd5f41a38'
'hur_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20330101-20351231.nc' 'http://aims3.llnl.gov/thredds/fileServer/css03_data/CMIP6/ScenarioMIP/BCC/BCC-CSM2-MR/ssp585/r1i1p1f1/day/hur/gn/v20190318/hur_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20330101-20351231.nc' 'SHA256' '9316a40ff223d9b7a752582b9b544848be84d47427e9e08172914fd01dc3d045'
'hur_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20360101-20381231.nc' 'http://aims3.llnl.gov/thredds/fileServer/css03_data/CMIP6/ScenarioMIP/BCC/BCC-CSM2-MR/ssp585/r1i1p1f1/day/hur/gn/v20190318/hur_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20360101-20381231.nc' 'SHA256' '2935e7b579a981e9ecb18debd847805578e2da1112714483497f146ea20b5270'
'hur_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20390101-20411231.nc' 'http://aims3.llnl.gov/thredds/fileServer/css03_data/CMIP6/ScenarioMIP/BCC/BCC-CSM2-MR/ssp585/r1i1p1f1/day/hur/gn/v20190318/hur_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20390101-20411231.nc' 'SHA256' 'd2cf8968a1a56411f96b7170d29da4d4091b4a471a55d356227beaa57b291713'
'hur_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20420101-20441231.nc' 'http://aims3.llnl.gov/thredds/fileServer/css03_data/CMIP6/ScenarioMIP/BCC/BCC-CSM2-MR/ssp585/r1i1p1f1/day/hur/gn/v20190318/hur_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20420101-20441231.nc' 'SHA256' 'a2ebc162b91707b7ffc02fe9306d54d8f08b17cfa50a1df419be9b42a9a200a6'
'hur_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20450101-20471231.nc' 'http://aims3.llnl.gov/thredds/fileServer/css03_data/CMIP6/ScenarioMIP/BCC/BCC-CSM2-MR/ssp585/r1i1p1f1/day/hur/gn/v20190318/hur_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20450101-20471231.nc' 'SHA256' '5548053ea172522de7bbd1d214e6f2ec5badb21d5b5643403c1e9ef7fda3c3b3'
'hur_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20480101-20501231.nc' 'http://aims3.llnl.gov/thredds/fileServer/css03_data/CMIP6/ScenarioMIP/BCC/BCC-CSM2-MR/ssp585/r1i1p1f1/day/hur/gn/v20190318/hur_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20480101-20501231.nc' 'SHA256' '4a27697c98320572108278351241ac527be2e1ed7b8370ee663061656bd3f3e0'
'hur_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20510101-20531231.nc' 'http://aims3.llnl.gov/thredds/fileServer/css03_data/CMIP6/ScenarioMIP/BCC/BCC-CSM2-MR/ssp585/r1i1p1f1/day/hur/gn/v20190318/hur_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20510101-20531231.nc' 'SHA256' '3fdc43c3c19a58f44eb6beacaeaae439d95594ee4011e0577d7cb1ca6c278edf'
'hur_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20540101-20561231.nc' 'http://aims3.llnl.gov/thredds/fileServer/css03_data/CMIP6/ScenarioMIP/BCC/BCC-CSM2-MR/ssp585/r1i1p1f1/day/hur/gn/v20190318/hur_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20540101-20561231.nc' 'SHA256' '7763e109710596a343895c6b6ebce28d02eb786cf859c8a74fec60a4c0f640eb'
'hur_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20570101-20591231.nc' 'http://aims3.llnl.gov/thredds/fileServer/css03_data/CMIP6/ScenarioMIP/BCC/BCC-CSM2-MR/ssp585/r1i1p1f1/day/hur/gn/v20190318/hur_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20570101-20591231.nc' 'SHA256' '984f1c2e3ba10e71081b83dd8da028a65679fca5d1f668440eec1b4649d82bcc'
'hur_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20600101-20621231.nc' 'http://aims3.llnl.gov/thredds/fileServer/css03_data/CMIP6/ScenarioMIP/BCC/BCC-CSM2-MR/ssp585/r1i1p1f1/day/hur/gn/v20190318/hur_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20600101-20621231.nc' 'SHA256' '3d59cc16d59f21ce2cb4e628819dd6d3effe51b4505d1675d7f8efd14be4c9d7'
'hur_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20630101-20651231.nc' 'http://aims3.llnl.gov/thredds/fileServer/css03_data/CMIP6/ScenarioMIP/BCC/BCC-CSM2-MR/ssp585/r1i1p1f1/day/hur/gn/v20190318/hur_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20630101-20651231.nc' 'SHA256' 'ff38c57826b0cc30a48009b66a71f773e3e3fcff67bdf787687d06e09ccc173f'
'hur_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20660101-20681231.nc' 'http://aims3.llnl.gov/thredds/fileServer/css03_data/CMIP6/ScenarioMIP/BCC/BCC-CSM2-MR/ssp585/r1i1p1f1/day/hur/gn/v20190318/hur_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20660101-20681231.nc' 'SHA256' 'fd9bda37c12e30ceef0a2e3ab04094c3bc49a239e9c978828e1a70754a1c5e7e'
'hur_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20690101-20711231.nc' 'http://aims3.llnl.gov/thredds/fileServer/css03_data/CMIP6/ScenarioMIP/BCC/BCC-CSM2-MR/ssp585/r1i1p1f1/day/hur/gn/v20190318/hur_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20690101-20711231.nc' 'SHA256' '42a02e60589958d8594dab8cff23060a7d0a65b49c235fe1535273782f007826'
'hur_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20720101-20741231.nc' 'http://aims3.llnl.gov/thredds/fileServer/css03_data/CMIP6/ScenarioMIP/BCC/BCC-CSM2-MR/ssp585/r1i1p1f1/day/hur/gn/v20190318/hur_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20720101-20741231.nc' 'SHA256' '6c77b766d73cbf5670642fc3abc2ec106681a480f0d80ade16e99981c84a8177'
'hur_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20750101-20771231.nc' 'http://aims3.llnl.gov/thredds/fileServer/css03_data/CMIP6/ScenarioMIP/BCC/BCC-CSM2-MR/ssp585/r1i1p1f1/day/hur/gn/v20190318/hur_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20750101-20771231.nc' 'SHA256' '036b34053c3b7ae72a1a0a8d574d3687aaba529ec5f872df3687c29bab58d173'
'hur_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20780101-20801231.nc' 'http://aims3.llnl.gov/thredds/fileServer/css03_data/CMIP6/ScenarioMIP/BCC/BCC-CSM2-MR/ssp585/r1i1p1f1/day/hur/gn/v20190318/hur_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20780101-20801231.nc' 'SHA256' 'f1a5fb507c6573007ab1c703fa8899f6975fd7f787ebf69d8b688066c62e590c'
'hur_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20810101-20831231.nc' 'http://aims3.llnl.gov/thredds/fileServer/css03_data/CMIP6/ScenarioMIP/BCC/BCC-CSM2-MR/ssp585/r1i1p1f1/day/hur/gn/v20190318/hur_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20810101-20831231.nc' 'SHA256' '53589ff5d094c8a9a071de1618223da2b371681d40f055387d75b550c9a929cf'
'hur_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20840101-20861231.nc' 'http://aims3.llnl.gov/thredds/fileServer/css03_data/CMIP6/ScenarioMIP/BCC/BCC-CSM2-MR/ssp585/r1i1p1f1/day/hur/gn/v20190318/hur_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20840101-20861231.nc' 'SHA256' 'b5987ad85d7b0a9650075f33462211f9ccab38d9b5857144171907b9c8f00c9e'
'hur_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20870101-20891231.nc' 'http://aims3.llnl.gov/thredds/fileServer/css03_data/CMIP6/ScenarioMIP/BCC/BCC-CSM2-MR/ssp585/r1i1p1f1/day/hur/gn/v20190318/hur_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20870101-20891231.nc' 'SHA256' 'c5c8a2130e9e0675c4bd0123989d26c5e8316363b04b4bf54d88453c629d0abe'
'hur_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20900101-20921231.nc' 'http://aims3.llnl.gov/thredds/fileServer/css03_data/CMIP6/ScenarioMIP/BCC/BCC-CSM2-MR/ssp585/r1i1p1f1/day/hur/gn/v20190318/hur_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20900101-20921231.nc' 'SHA256' '16194ec02a1211dc3da8018181ddd545525cfceda6697d93c1777ba2a52ce0b3'
'hur_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20930101-20951231.nc' 'http://aims3.llnl.gov/thredds/fileServer/css03_data/CMIP6/ScenarioMIP/BCC/BCC-CSM2-MR/ssp585/r1i1p1f1/day/hur/gn/v20190318/hur_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20930101-20951231.nc' 'SHA256' '5fe494bbcb676814153d9975952185fba614e52ca576cba58e37965931c41d31'
'hur_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20960101-20981231.nc' 'http://aims3.llnl.gov/thredds/fileServer/css03_data/CMIP6/ScenarioMIP/BCC/BCC-CSM2-MR/ssp585/r1i1p1f1/day/hur/gn/v20190318/hur_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20960101-20981231.nc' 'SHA256' 'dcb8f9714e80b6d4ef9fabaa6f3cb1d7af766d45f4d32cbb6f9a3b9adf7fd485'
'hur_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20990101-21001231.nc' 'http://aims3.llnl.gov/thredds/fileServer/css03_data/CMIP6/ScenarioMIP/BCC/BCC-CSM2-MR/ssp585/r1i1p1f1/day/hur/gn/v20190318/hur_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20990101-21001231.nc' 'SHA256' '08681e55bae0155e68b3363723abd0b1953de1533e53b7065102ed418908c637'
'pr_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20150101-20391231.nc' 'http://aims3.llnl.gov/thredds/fileServer/css03_data/CMIP6/ScenarioMIP/BCC/BCC-CSM2-MR/ssp585/r1i1p1f1/day/pr/gn/v20190318/pr_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20150101-20391231.nc' 'SHA256' '3991c2f174da50899ba394f93c5730ce179e7015815aee8e7caf295ce31fe5c9'
'pr_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20400101-20641231.nc' 'http://aims3.llnl.gov/thredds/fileServer/css03_data/CMIP6/ScenarioMIP/BCC/BCC-CSM2-MR/ssp585/r1i1p1f1/day/pr/gn/v20190318/pr_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20400101-20641231.nc' 'SHA256' 'b21dd40d777866a4fc5fa052e7020563027ad93d9f738facd64a2f1383410ca2'
'pr_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20650101-20891231.nc' 'http://aims3.llnl.gov/thredds/fileServer/css03_data/CMIP6/ScenarioMIP/BCC/BCC-CSM2-MR/ssp585/r1i1p1f1/day/pr/gn/v20190318/pr_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20650101-20891231.nc' 'SHA256' '4d775f3b5cf21e5fd47328ea3a58936c7753395105d608dd3ff08e61d5aa4aa3'
'pr_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20900101-21001231.nc' 'http://aims3.llnl.gov/thredds/fileServer/css03_data/CMIP6/ScenarioMIP/BCC/BCC-CSM2-MR/ssp585/r1i1p1f1/day/pr/gn/v20190318/pr_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20900101-21001231.nc' 'SHA256' 'd5623082d336e176c12afb857cecb70bef53c99073f75fa5f8323dc19908d3de'
'rsds_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20150101-20391231.nc' 'http://aims3.llnl.gov/thredds/fileServer/css03_data/CMIP6/ScenarioMIP/BCC/BCC-CSM2-MR/ssp585/r1i1p1f1/day/rsds/gn/v20190318/rsds_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20150101-20391231.nc' 'SHA256' '179e642ed2c9855cce5085fc029161b3677a167c54f606c65b4860a201115631'
'rsds_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20400101-20641231.nc' 'http://aims3.llnl.gov/thredds/fileServer/css03_data/CMIP6/ScenarioMIP/BCC/BCC-CSM2-MR/ssp585/r1i1p1f1/day/rsds/gn/v20190318/rsds_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20400101-20641231.nc' 'SHA256' '1880539d22f54b90c3dd325ef8f51ad2848584c84ec88e6acbd573c58442abce'
'rsds_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20650101-20891231.nc' 'http://aims3.llnl.gov/thredds/fileServer/css03_data/CMIP6/ScenarioMIP/BCC/BCC-CSM2-MR/ssp585/r1i1p1f1/day/rsds/gn/v20190318/rsds_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20650101-20891231.nc' 'SHA256' '08dedd33a1329ef1394763dd27d5b31483d79d2e6e9ef2f4326881a202b05c42'
'rsds_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20900101-21001231.nc' 'http://aims3.llnl.gov/thredds/fileServer/css03_data/CMIP6/ScenarioMIP/BCC/BCC-CSM2-MR/ssp585/r1i1p1f1/day/rsds/gn/v20190318/rsds_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20900101-21001231.nc' 'SHA256' 'df4cca4bc4d838408af049a6fb19201ae65a13c16cdc62aa9834a0ab3e077b4b'
'sfcWind_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20150101-20391231.nc' 'http://aims3.llnl.gov/thredds/fileServer/css03_data/CMIP6/ScenarioMIP/BCC/BCC-CSM2-MR/ssp585/r1i1p1f1/day/sfcWind/gn/v20190318/sfcWind_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20150101-20391231.nc' 'SHA256' '825f8617791c846d333bd4f4b8b1189ac10dcc3d9daa2b0de99e8dc0ed3f6740'
'sfcWind_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20400101-20641231.nc' 'http://aims3.llnl.gov/thredds/fileServer/css03_data/CMIP6/ScenarioMIP/BCC/BCC-CSM2-MR/ssp585/r1i1p1f1/day/sfcWind/gn/v20190318/sfcWind_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20400101-20641231.nc' 'SHA256' '4a473fef26e72820ad35519209a605f00f0b8af24af66ddf8b77f39559af094a'
'sfcWind_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20650101-20891231.nc' 'http://aims3.llnl.gov/thredds/fileServer/css03_data/CMIP6/ScenarioMIP/BCC/BCC-CSM2-MR/ssp585/r1i1p1f1/day/sfcWind/gn/v20190318/sfcWind_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20650101-20891231.nc' 'SHA256' '68c5a3175a73277ef8c2a0d212eaa4bc3b9d0bc5564b48916bcd9c1740c0a881'
'sfcWind_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20900101-21001231.nc' 'http://aims3.llnl.gov/thredds/fileServer/css03_data/CMIP6/ScenarioMIP/BCC/BCC-CSM2-MR/ssp585/r1i1p1f1/day/sfcWind/gn/v20190318/sfcWind_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20900101-21001231.nc' 'SHA256' '2c7bc805d4df9b14310b43c021acd7506954ebf1cf49a9e2dd4ee443bc8dc31d'
'tas_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20150101-20391231.nc' 'http://aims3.llnl.gov/thredds/fileServer/css03_data/CMIP6/ScenarioMIP/BCC/BCC-CSM2-MR/ssp585/r1i1p1f1/day/tas/gn/v20190318/tas_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20150101-20391231.nc' 'SHA256' '84642477e5d0d25a81a4210e642e7e73b8279a33462f9595e635b89feae0aefb'
'tas_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20400101-20641231.nc' 'http://aims3.llnl.gov/thredds/fileServer/css03_data/CMIP6/ScenarioMIP/BCC/BCC-CSM2-MR/ssp585/r1i1p1f1/day/tas/gn/v20190318/tas_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20400101-20641231.nc' 'SHA256' '604044457c88c9bc187363b419f2df68ca403c90d4427df730569063bd73db9b'
'tas_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20650101-20891231.nc' 'http://aims3.llnl.gov/thredds/fileServer/css03_data/CMIP6/ScenarioMIP/BCC/BCC-CSM2-MR/ssp585/r1i1p1f1/day/tas/gn/v20190318/tas_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20650101-20891231.nc' 'SHA256' '990281af1e49e0feafc6c0d714e75ff8fcb36d2b5b703df722d497288ba1c463'
'tas_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20900101-21001231.nc' 'http://aims3.llnl.gov/thredds/fileServer/css03_data/CMIP6/ScenarioMIP/BCC/BCC-CSM2-MR/ssp585/r1i1p1f1/day/tas/gn/v20190318/tas_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20900101-21001231.nc' 'SHA256' '48d9c743dfcaec2387394b379dcbfb6478be87cd25408a3d9150f6ce12ef216b'
'tasmax_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20150101-20391231.nc' 'http://aims3.llnl.gov/thredds/fileServer/css03_data/CMIP6/ScenarioMIP/BCC/BCC-CSM2-MR/ssp585/r1i1p1f1/day/tasmax/gn/v20190318/tasmax_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20150101-20391231.nc' 'SHA256' '186792539b7070f695ac0b5463a6fc3dde8d31fb6f03e9ee926ba033213e00fb'
'tasmax_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20400101-20641231.nc' 'http://aims3.llnl.gov/thredds/fileServer/css03_data/CMIP6/ScenarioMIP/BCC/BCC-CSM2-MR/ssp585/r1i1p1f1/day/tasmax/gn/v20190318/tasmax_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20400101-20641231.nc' 'SHA256' 'c83ef80d2fb041bb204e11d2b86fd978be5d09d76046de5d6ddb4cda8729d6be'
'tasmax_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20650101-20891231.nc' 'http://aims3.llnl.gov/thredds/fileServer/css03_data/CMIP6/ScenarioMIP/BCC/BCC-CSM2-MR/ssp585/r1i1p1f1/day/tasmax/gn/v20190318/tasmax_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20650101-20891231.nc' 'SHA256' 'fc36e7c813c1f3c6e107934f5590603ef3ef1ac65144b337b37688eb7180d941'
'tasmax_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20900101-21001231.nc' 'http://aims3.llnl.gov/thredds/fileServer/css03_data/CMIP6/ScenarioMIP/BCC/BCC-CSM2-MR/ssp585/r1i1p1f1/day/tasmax/gn/v20190318/tasmax_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20900101-21001231.nc' 'SHA256' '841b119116f607d528249d0616ceee06de313ccb3e83ccdaaa4e6ac14020da6f'
'tasmin_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20150101-20391231.nc' 'http://aims3.llnl.gov/thredds/fileServer/css03_data/CMIP6/ScenarioMIP/BCC/BCC-CSM2-MR/ssp585/r1i1p1f1/day/tasmin/gn/v20190318/tasmin_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20150101-20391231.nc' 'SHA256' 'c68953c2fa97a495ea65dc74faae4c001cfac0e68b98f415555cc75dcbf63d2b'
'tasmin_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20400101-20641231.nc' 'http://aims3.llnl.gov/thredds/fileServer/css03_data/CMIP6/ScenarioMIP/BCC/BCC-CSM2-MR/ssp585/r1i1p1f1/day/tasmin/gn/v20190318/tasmin_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20400101-20641231.nc' 'SHA256' '0efbfd302e2e2f1cbbc7bc4814dac8d12bb029d8a1216ad7188eac565635189e'
'tasmin_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20650101-20891231.nc' 'http://aims3.llnl.gov/thredds/fileServer/css03_data/CMIP6/ScenarioMIP/BCC/BCC-CSM2-MR/ssp585/r1i1p1f1/day/tasmin/gn/v20190318/tasmin_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20650101-20891231.nc' 'SHA256' 'bd8df0d9f6102ea765d414f620ad8a30bb4fe2375dc5ee7ee82b7229fb55c21d'
'tasmin_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20900101-21001231.nc' 'http://aims3.llnl.gov/thredds/fileServer/css03_data/CMIP6/ScenarioMIP/BCC/BCC-CSM2-MR/ssp585/r1i1p1f1/day/tasmin/gn/v20190318/tasmin_day_BCC-CSM2-MR_ssp585_r1i1p1f1_gn_20900101-21001231.nc' 'SHA256' '70c91a559ab6048c4e6d92c02ac4f3dc5c61c1f40e986d06280b1869e4b1536a'
EOF--dataset.file.url.chksum_type.chksum
)"

# ESG_HOME should point to the directory containing ESG credentials.
#   Default is $HOME/.esg
ESG_HOME=${ESG_HOME:-$HOME/.esg}
[[ -d $ESG_HOME ]] || mkdir -p $ESG_HOME

ESG_CREDENTIALS=${X509_USER_PROXY:-$ESG_HOME/credentials.pem}
ESG_CERT_DIR=${X509_CERT_DIR:-$ESG_HOME/certificates}
MYPROXY_STATUS=$HOME/.MyProxyLogon
COOKIE_JAR=$ESG_HOME/cookies
MYPROXY_GETCERT=$ESG_HOME/getcert.jar
CERT_EXPIRATION_WARNING=$((60 * 60 * 8))   #Eight hour (in seconds)

WGET_TRUSTED_CERTIFICATES=$ESG_HOME/certificates


# Configure checking of server SSL certificates.
#   Disabling server certificate checking can resolve problems with myproxy
#   servers being out of sync with datanodes.
CHECK_SERVER_CERT=${CHECK_SERVER_CERT:-Yes}

check_os() {
    local os_name=$(uname | awk '{print $1}')
    case ${os_name} in
        Linux)
            ((debug)) && echo "Linux operating system detected"
            LINUX=1
            MACOSX=0
            ;;
        Darwin)
            ((debug)) && echo "Mac OS X operating system detected"
            LINUX=0
            MACOSX=1
            ;;
        *)
            echo "Unrecognized OS [${os_name}]"
            return 1
            ;;
    esac
    return 0
}

#taken from http://stackoverflow.com/a/4025065/1182464
vercomp () {
    if [[ $1 == $2 ]]
    then
        return 0
    fi
    local IFS=.
    local i ver1=($1) ver2=($2)
    # fill empty fields in ver1 with zeros
    for ((i=${#ver1[@]}; i<${#ver2[@]}; i++))
    do
        ver1[i]=0
    done
    for ((i=0; i<${#ver1[@]}; i++))
    do
        if [[ -z ${ver2[i]} ]]
        then
            # fill empty fields in ver2 with zeros
            ver2[i]=0
        fi
        if ((10#${ver1[i]} > 10#${ver2[i]}))
        then
            return 1
        fi
        if ((10#${ver1[i]} < 10#${ver2[i]}))
        then
            return 2
        fi
    done
    return 0
}

check_commands() {
    #check wget
    local MIN_WGET_VERSION=1.10
    vercomp $(wget -V | sed -n 's/^.* \([1-9]\.[0-9.]*\) .*$/\1/p') $MIN_WGET_VERSION
    case $? in
        2) #lower
            wget -V
            echo
            echo "** ERROR: wget version is too old. Use version $MIN_WGET_VERSION or greater. **" >&2
            exit 1
    esac
}

usage() {
    echo "Usage: $(basename $0) [flags] [openid] [username]"
    echo "Flags is one of:"
    sed -n '/^while getopts/,/^done/  s/^\([^)]*\)[^#]*#\(.*$\)/\1 \2/p' $0
    echo
    echo "This command stores the states of the downloads in .$0.status"
    echo "For more information check the website: http://esgf.org/wiki/ESGF_wget"
}

#defaults
debug=0
clean_work=1

#parse flags
while getopts ':c:pfF:o:w:isuUndvqhHI:T' OPT; do
    case $OPT in
        H) skip_security=1 && use_http_sec=1;; #       : Authenticate with OpenID (username,) and password, without the need for a certificate.
        T) force_TLSv1=1;;              #       : Forces wget to use TLSv1. 
        c) ESG_CREDENTIALS="$OPTARG";;  #<cert> : use this certificate for authentication.
        f) force=1;;                    #       : force certificate retrieval (defaults to only once per day); for certificate-less authentication (see -H option), this flag will force login and refresh cookies.
        F) input_file="$OPTARG";;       #<file> : read input from file instead of the embedded one (use - to read from stdin)
        o) openId="$OPTARG";;           #<openid>: Provide OpenID instead of interactively asking for it.
        I) username_supplied="$OPTARG";;    #<user_id> : Explicitly set user ID.  By default, the user ID is extracted from the last component of the OpenID URL.  Use this flag to override this behaviour.                   
        w) output="$OPTARG";;           #<file> : Write embedded files into a file and exit
        i) insecure=1;;                 #       : set insecure mode, i.e. don't check server certificate
        s) skip_security=1 && use_cookies_for_http_basic_auth_start=1;;            #       : completely skip security. It will only work if the accessed data is not secured at all. -- works only if the accessed data is unsecured or a certificate exists or cookies are saved (latter applies to -H option only).
        u) update=1;;                   #       : Issue the search again and see if something has changed.
        U) update_files=1;;             #       : Update files from server overwriting local ones (detect with -u)
        n) dry_run=1;;                  #       : Don't download any files, just report.
        p) clean_work=0;;               #       : preserve data that failed checksum
        d) verbose=1;debug=1;;          #       : display debug information
        v) verbose=1;;                  #       : be more verbose
        q) quiet=1;;                    #       : be less verbose
        h) usage && exit 0;;            #       : displays this help
        \?) echo "Unknown option '$OPTARG'" >&2 && usage && exit 1;;
        \:) echo "Missing parameter for flag '$OPTARG'" >&2 && usage && exit 1;;
    esac
done
shift $(($OPTIND - 1))

#setup input as desired by the user
if [[ "$input_file" ]]; then
    if [[ "$input_file" == '-' ]]; then
        download_files="$(cat)" #read from STDIN
        exec 0</dev/tty #reopen STDIN as cat closed it
    else
        download_files="$(cat $input_file)" #read from file
    fi
fi

#if -w (output) was selected write file and finish:
if [[ "$output" ]]; then
    #check the file
    if [[ -f "$output" ]]; then
        read -p "Overwrite existing file $output? (y/N) " answ
        case $answ in y|Y|yes|Yes);; *) echo "Aborting then..."; exit 0;; esac
    fi
    echo "$download_files">$output
    exit
fi


#assure we have everything we need
check_commands

if ((update)); then
    echo "Checking the server for changes..."
    new_wget="$(wget "$search_url" -qO -)"
    compare_cmd="grep -vE '^(# Generated by|# Search URL|search_url=)'"
    if diff -q <(eval $compare_cmd<<<"$new_wget") <(eval $compare_cmd $0) >/dev/null; then
        echo "No changes detected."
    else
        echo "Wget was changed. Dowloading. (old renamed to $0.old.#N)"
        counter=0
        while [[ -f $0.old.$counter ]]; do ((counter++)); done
        mv $0 $0.old.$counter
        echo "$new_wget" > $0
    fi
    exit 0      
fi


##############################################################################
check_java() {
    if ! type java >& /dev/null; then
        echo "Java could not be found." >&2
        return 1
    fi
    if java -version 2>&1|grep openjdk >/dev/null; then
        openjdk=1;
    else
        openjdk=0;
    fi
    jversion=($(jversion=$(java -version 2>&1 | awk '/version/ {gsub("\"","");print $3}'); echo ${jversion//./ }))
    mVer=${jversion[1]}
    if [ $openjdk -eq 1 ]; then
        mVer=${jversion[0]}
        if ((mVer<5)); then
            echo "Openjdk detected. Version 9+ is required for retrieving the certificate." >&2
            echo "Current version seems older: $(java -version | head -n1) " >&2
            return 1
        fi
    else
    
        if ((mVer<5)); then
            echo "Java version 1.5+ is required for retrieving the certificate." >&2
            echo "Current version seems older: $(java -version | head -n1) " >&2
            return 1
        fi
    fi
}

check_myproxy_logon() {
    if ! type myproxy-logon >& /dev/null; then
	echo "myproxy-logon could not be found." >&2
	return 1
    fi
    echo "myproxy-logon found" >&2
}

proxy_to_java() {
    local proxy_user proxy_pass proxy_server proxy_port
    eval $(sed 's#^\(https\?://\)\?\(\([^:@]*\)\(:\([^@]*\)\)\?@\)\?\([^:/]*\)\(:\([0-9]*\)\)\?.*#proxy_user=\3;proxy_pass=\5;proxy_server=\6;proxy_port=\8#'<<<$http_proxy)
    local JAVA_PROXY=
    [[ "$proxy_server" ]] && JAVA_PROXY=$JAVA_PROXY" -Dhttp.proxyHost=$proxy_server"
    [[ "$proxy_port" ]] && JAVA_PROXY=$JAVA_PROXY" -Dhttp.proxyPort=$proxy_port"
    eval $(sed 's#^\(https\?://\)\?\(\([^:@]*\)\(:\([^@]*\)\)\?@\)\?\([^:/]*\)\(:\([0-9]*\)\)\?.*#proxy_user=\3;proxy_pass=\5;proxy_server=\6;proxy_port=\8#'<<<$https_proxy)
    [[ "$proxy_server" ]] && JAVA_PROXY=$JAVA_PROXY" -Dhttps.proxyHost=$proxy_server"
    [[ "$proxy_port" ]] && JAVA_PROXY=$JAVA_PROXY" -Dhttps.proxyPort=$proxy_port"
    
    echo "$JAVA_PROXY"
}

# get certificates from github
get_certificates() {
    # don't if this was already done today
    [[ -z $force && "$(find $ESG_CERT_DIR -type d -mtime -1 2>/dev/null)" ]] && return 0
    echo -n "Retrieving Federation Certificates..." >&2

    if ! wget -O $ESG_HOME/esg-truststore.ts --no-check-certificate https://github.com/ESGF/esgf-dist/raw/master/installer/certs/esg-truststore.ts; then
        echo "Could not fetch esg-truststore";
        return 1
    fi
    
    if ! wget --no-check-certificate https://raw.githubusercontent.com/ESGF/esgf-dist/master/installer/certs/esg_trusted_certificates.tar -O - -q | tar x -C $ESG_HOME; then
        #certificates tarred into esg_trusted_certificates. (if it breaks, let the user know why
        wget --no-check-certificate https://raw.githubusercontent.com/ESGF/esgf-dist/master/installer/certs/esg_trusted_certificates.tar
        echo "Could't update certs!" >&2
        return 1
    else
        #if here everythng went fine. Replace old cert with this ones    
        [[ -d $ESG_CERT_DIR ]] && rm -r $ESG_CERT_DIR || mkdir -p $(dirname $ESG_CERT_DIR)
        mv $ESG_HOME/esg_trusted_certificates $ESG_CERT_DIR
        touch $ESG_CERT_DIR
        echo "done!" >&2
    fi

}

# Retrieve ESG credentials
unset pass
get_credentials() {
    if check_java
    then
	use_java=1
    else	
	use_java=0
	echo "No suitable java for obtaining certificate - checking for myproxy-logon instead" >&2
	check_myproxy_logon || exit 1
    fi
    #get all certificates
    get_certificates

    if [[ -z "$(find $MYPROXY_GETCERT -type f -mtime -1 2>/dev/null)" ]]; then
        echo -n "(Downloading $MYPROXY_GETCERT... "
        mkdir -p $(dirname $MYPROXY_GETCERT)
        if wget -q --no-check-certificate https://raw.githubusercontent.com/ESGF/esgf-dist/master/installer/certs/getcert.jar -O $MYPROXY_GETCERT;then
            echo 'done)'
            touch $MYPROXY_GETCERT
        else
            echo 'failed)'
        fi
    fi

    #if the user already defined one, use it
    if [[ -z $openId ]]; then
        #try to parse the last valid value if any
        [[ -f "$MYPROXY_STATUS" ]] && openId=$(awk -F= '/^OpenID/ {gsub("\\\\", ""); print $2}' $MYPROXY_STATUS)
        if [[ -z $openId ]]; then
            #no OpenID, we need to ask the user
            echo -n "Please give your OpenID (Example: https://myserver/example/username) ? "
        else
            #Allow the user to change it if desired
            echo -n "Please give your OpenID (hit ENTER to accept default: $openId)? "
        fi
        read -e
        [[ "$REPLY" ]] && openId="$REPLY"
    else
        ((verbose)) && echo "Using user defined OpenID $openId (to change use -o <open_id>)"
    fi

    if grep -q ceda.ac.uk <<<$openId; then
        username=${openId##*/}
        echo -n "Please give your username if different [$username]: "
        read -e
        [[ "$REPLY" ]] && username="$REPLY"
    fi
    


    if [ $use_java -eq 1 ]
    then
        local args=
        #get password
	[[ ! "$pass" ]] && read -sp "MyProxy Password? " pass
        [[ "$openId" ]] && args=$args" --oid $openId"
        [[ "$pass" ]] && args=$args" -P $pass"
        [[ "$username" ]] && args=$args" -l $username"
        
        echo -n $'\nRetrieving Credentials...' >&2
        if ! java $(proxy_to_java) -jar $MYPROXY_GETCERT $args --ca-directory $ESG_CERT_DIR --output $ESG_CREDENTIALS ; then        
            echo "Certificate could not be retrieved"
            exit 1
        fi
        echo "done!" >&2
    else
        args=`openid_to_myproxy_args $openId $username` || exit 1
        if ! myproxy-logon $args -b -o $ESG_CREDENTIALS
	then
            echo "Certificate could not be retrieved"
	    exit 1
        fi
	cp $HOME/.globus/certificates/* $ESG_CERT_DIR/	
    fi
}

openid_to_myproxy_args() {
  python - <<EOF || exit 1
import sys
import re
import xml.etree.ElementTree as ET
import urllib2
openid = "$1"
username = "$2" or re.sub(".*/", "", openid)
e = ET.parse(urllib2.urlopen(openid))
servs = [el for el in e.getiterator() if el.tag.endswith("Service")]
for serv in servs:
    servinfo = dict([(re.sub(".*}", "", c.tag), c.text)
                     for c in serv.getchildren()])
    try:
        if servinfo["Type"].endswith("myproxy-service"):
            m = re.match("socket://(.*):(.*)", servinfo["URI"])
            if m:
                host = m.group(1)
                port = m.group(2)
                print "-s %s -p %s -l %s" % (host, port, username)
                break
    except KeyError:
        continue
else:
    sys.stderr.write("myproxy service could not be found\n")
    sys.exit(1)
EOF
}

# check the certificate validity
check_cert() {
    if [[ ! -f "$ESG_CERT" || $force ]]; then
        #not there, just get it
        get_credentials
    elif which openssl &>/dev/null; then
        #check openssl and certificate
        if ! openssl x509 -checkend $CERT_EXPIRATION_WARNING -noout -in $ESG_CERT 2>/dev/null; then
            echo "The certificate expires in less than $((CERT_EXPIRATION_WARNING / 60 / 60)) hour(s). Renewing..."
            get_credentials
        else
            #ok, certificate is fine
            return 0
        fi
    fi
}

#
# Detect ESG credentials
#
find_credentials() {

    #is X509_USER_PROXY or $HOME/.esg/credential.pem
    if [[ -f "$ESG_CREDENTIALS" ]]; then
        # file found, proceed.
        ESG_CERT="$ESG_CREDENTIALS"
        ESG_KEY="$ESG_CREDENTIALS"
    elif [[ -f "$X509_USER_CERT" && -f "$X509_USER_KEY" ]]; then
        # second try, use these certificates.
        ESG_CERT="$X509_USER_CERT"
        ESG_KEY="$X509_USER_KEY"
    else
        # If credentials are not present, just point to where they should go 
        echo "No ESG Credentials found in $ESG_CREDENTIALS" >&2
            ESG_CERT="$ESG_CREDENTIALS"
            ESG_KEY="$ESG_CREDENTIALS"
            #they will be retrieved later one
    fi


    #chek openssl and certificate
    if (which openssl &>/dev/null); then
        if ( openssl version | grep 'OpenSSL 1\.0' ); then
            echo '** WARNING: ESGF Host certificate checking might not be compatible with OpenSSL 1.0+'
        fi
        check_cert || { (($?==1)); exit 1; }
    fi
    
    if [[ $CHECK_SERVER_CERT == "Yes" ]]; then
        [[ -d "$ESG_CERT_DIR" ]] || { echo "CA certs not found. Aborting."; exit 1; }
        PKI_WGET_OPTS="--ca-directory=$ESG_CERT_DIR"
    fi

    #some wget version complain if there's no file present
    [[ -f $COOKIE_JAR ]] || touch $COOKIE_JAR

    PKI_WGET_OPTS="$PKI_WGET_OPTS --certificate=$ESG_CERT --private-key=$ESG_KEY --save-cookies=$COOKIE_JAR --load-cookies=$COOKIE_JAR --ca-certificate=$ESG_CERT"

}

check_chksum() {
    local file="$1"
    local chk_type=$2
    local chk_value=$3
    local local_chksum=Unknown

    case $chk_type in
        md5) local_chksum=$(md5sum_ $file | cut -f1 -d" ");;
        sha256) local_chksum=$(sha256sum_ $file|awk '{print $1}'|cut -d ' ' -f1);;
        *) echo "Can't verify checksum." && return 0;;
    esac

    #verify
    ((debug)) && echo "local:$local_chksum vs remote:$chk_value" >&2
    echo $local_chksum
}

#Our own md5sum function call that takes into account machines that don't have md5sum but do have md5 (i.e. mac os x)
md5sum_() {
    hash -r
    if type md5sum >& /dev/null; then
        echo $(md5sum $@)
    else
        echo $(md5 $@ | sed -n 's/MD5[ ]*\(.*\)[^=]*=[ ]*\(.*$\)/\2 \1/p')
    fi
}

#Our own sha256sum function call that takes into account machines that don't have sha256sum but do have sha2 (i.e. mac os x)
sha256sum_() {
    hash -r
    if type sha256sum >& /dev/null; then
        echo $(sha256sum $@)
    elif type shasum >& /dev/null; then
        echo $(shasum -a 256 $@)
    else
        echo $(sha2 -q -256 $@)
    fi
}

get_mod_time_() {
    if ((MACOSX)); then
        #on a mac modtime is stat -f %m <file>
        echo "$(stat -f %m $@)"
    else
        #on linux (cygwin) modtime is stat -c %Y <file>
        echo "$(stat -c %Y $@)"
    fi
    return 0;
}

remove_from_cache() {
    local entry="$1"
    local tmp_file="$(grep -ve "^$entry" "$CACHE_FILE")"
    echo "$tmp_file" > "$CACHE_FILE"
    unset cached
}

#Download data from node using cookies and not certificates.
download_http_sec()
{
  #The data to be downloaded.
  data=" $url"
  filename="$file"  

  #Wget args.
  if ((insecure)) 
  then
   wget_args=" --no-check-certificate --cookies=on  --keep-session-cookies --save-cookies $COOKIES_FOLDER/wcookies.txt " 
  else
   wget_args=" --ca-directory=$WGET_TRUSTED_CERTIFICATES --cookies=on --keep-session-cookies --save-cookies $COOKIES_FOLDER/wcookies.txt "  
  fi 

  if ((use_cookies_for_http_basic_auth_start)) || ((use_cookies_for_http_basic_auth)) 
  then
   wget_args=" $wget_args"" --load-cookies $COOKIES_FOLDER/wcookies.txt"    
  fi
  
  if((force_TLSv1))
  then
   wget_args=" $wget_args"" --secure-protocol=TLSv1 "
  fi
  
  
  if [[ ! -z "$ESGF_WGET_OPTS" ]]
  then
    wget_args="$wget_args $ESGF_WGET_OPTS"
  fi  
  

  #use cookies for the next downloads
  use_cookies_for_http_basic_auth=1;
   
  #Debug message.
  if  ((debug))
  then
   echo -e "\nExecuting:\n"
   echo -e "wget $wget_args $data\n"
  fi


  #Try to download the data. 
  command="wget $wget_args -O $filename $data"
  http_resp=$(eval $command  2>&1) 
  cmd_exit_status="$?"
  
  if ((debug))
  then
   echo -e "\nHTTP response:\n $http_resp\n"
  fi
      
  #Extract orp service from url ?
  #Evaluate response.
  #redirects=$(echo "$http_resp" | egrep -c ' 302 ')
  #(( "$redirects" == 1 )) && 
  if  echo "$http_resp" | grep -q "/esg-orp/"      
  then
   urls=$(echo "$http_resp" | egrep -o 'https://[^ ]+' | cut -d'/' -f 3)
   orp_service=$(echo "$urls" | tr '\n' ' ' | cut -d' ' -f 2)


   #Use cookies for transaction with orp.
   wget_args=" $wget_args"" --load-cookies $COOKIES_FOLDER/wcookies.txt"    
   
   #Download data using either http basic auth or http login form.
   if [[ "$openid_c" == */openid/  || "$openid_c" == */openid ]]
   then
    download_http_sec_open_id
   else
    download_http_sec_decide_service
   fi
  else  
   if    echo "$http_resp" | grep -q "401 Unauthorized"  \
      || echo "$http_resp" | grep -q "403: Forbidden"  \
      || echo "$http_resp" | grep -q "Connection timed out."  \
      || echo "$http_resp" | grep -q "no-check-certificate"  \
      || (( $cmd_exit_status != 0 ))      
   then 
    echo "ERROR : http request to OpenID Relying Party service failed."
    failed=1
   fi
  fi
}


#Function that decides which implementaion of idp to use.
download_http_sec_decide_service()
{
  #find claimed id

  pos=$(echo "$openid_c" | egrep -o '/' | wc -l)
  username_c=$(echo "$openid_c"  | cut -d'/' -f "$(($pos + 1))")
  esgf_uri=$(echo "$openid_c" | egrep -o '/esgf-idp/openid/')

  host=$(echo "$openid_c"  | cut -d'/' -f 3)
  #test ceda first.

  if [[ -z "$esgf_uri" ]]
  then
   openid_c_tmp="https://""$host""/openid/"
  else
   openid_c_tmp="https://""$host""/esgf-idp/openid/" 
  fi

  command="wget "$openid_c_tmp" --no-check-certificate ${force_TLSv1:+--secure-protocol=TLSv1} -O-"
        
  if [[ ! -z "$ESGF_WGET_OPTS" ]]
  then
   command="$command $ESGF_WGET_OPTS"
  fi  
          
  #Debug message.
  if  ((debug))
  then
   echo -e "\nExecuting:\n"
   echo -e "$command\n"
  fi
            

  #Execution of command.
  http_resp=$(eval $command  2>&1)
  cmd_exit_status="$?"
  
  
  if ((debug))
  then
   echo -e "\nHTTP response:\n $http_resp\n"
  fi 
  

  if    echo "$http_resp" | grep -q "[application/xrds+xml]"  \
     && echo "$http_resp" | grep -q "200 OK"  \
     && (( cmd_exit_status == 0 ))       
  then
   openid_c=$openid_c_tmp
   download_http_sec_open_id
  else
   if [[ -z "$esgf_uri" ]]
   then
    echo "ERROR : HTTP request to OpenID Relying Party service failed."
    failed=1
   else
   download_http_sec_cl_id
   fi
  fi
}


download_http_sec_retry()
{
  echo -e "\nRetrying....\n"
  #Retry in case that last redirect did not work, this happens with older version of wget.
  command="wget $wget_args $data"
      
  #Debug message.
  if  ((debug))
  then
   echo -e "Executing:\n"
   echo -e "$command\n"
  fi   
   
  http_resp=$(eval $command  2>&1) 
  cmd_exit_status="$?"

  if ((debug))
  then
   echo -e "\nHTTP response:\n $http_resp\n"
  fi
   
  if    echo "$http_resp" | grep -q "401 Unauthorized"  \
     || echo "$http_resp" | grep -q "403: Forbidden"  \
     || echo "$http_resp" | grep -q "Connection timed out."  \
     || echo "$http_resp" | grep -q "no-check-certificate"  \
     || (( $cmd_exit_status != 0 ))      
  then 
   echo -e "\nERROR : Retry failed.\n"
   #rm "$filename"
   failed=1
  fi #if retry failed.
}

#Function for downloading data using the claimed id.
download_http_sec_cl_id()
{
  #Http request for sending openid to the orp service.
  command="wget --post-data \"openid_identifier=$openid_c&rememberOpenid=on\"  $wget_args -O- https://$orp_service/esg-orp/j_spring_openid_security_check.htm "

  #Debug message.
  if  ((debug))
  then
   echo -e "Executing:\n"
   echo -e "wget $command\n"
  fi 
  
  
  #Execution of command.
  http_resp=$(eval $command  2>&1)
  cmd_exit_status="$?"

  
  if ((debug))
  then
   echo -e "\nHTTP response:\n $http_resp\n"
  fi 
    
  
  #Extract orp service from openid ?
  #Evaluate response.If redirected to idp service send the credentials.
  #redirects=$(echo "$http_resp" | egrep -c ' 302 ')
  #(( redirects == 2  )) && 
  if  echo "$http_resp" | grep -q "login.htm"  && (( cmd_exit_status == 0 ))   
  then 
  
   urls=$(echo "$http_resp" | egrep -o 'https://[^ ]+' | cut -d'/' -f 3)
   idp_service=$(echo "$urls"  | tr '\n' ' ' | cut -d' ' -f 2) 
      
   command="wget --post-data  password=\"$password_c\" $wget_args ${quiet:+-q} ${quiet:--v} -O $filename https://$idp_service/esgf-idp/idp/login.htm"
   

   #Debug message.
   if  ((debug))
   then
    echo -e "Executing:\n"
    echo -e "wget $command\n"
   fi 

   #Execution of command.
   http_resp=$(eval $command  2>&1)
   cmd_exit_status="$?"
      
   if ((debug))
   then
    echo -e "\nHTTP response:\n $http_resp\n"
   fi 
        
   #Evaluate response. 
   #redirects=$(echo "$http_resp" | egrep -c ' 302 ')
   #(( "$redirects" != 5 )) \ 
   if    echo "$http_resp" | grep -q "text/html"  \
      || echo "$http_resp" | grep -q "403: Forbidden"  \
      || (( cmd_exit_status != 0 ))        
   then 
    rm "$filename"
    download_http_sec_retry
   fi
 
  else
   echo "ERROR : HTTP request to OpenID Provider service failed."
   failed=1
  fi #if redirected to idp.  
}



download_http_sec_open_id()
{
  #Http request for sending openid to the orp web service.
  command="wget --post-data \"openid_identifier=$openid_c&rememberOpenid=on\" --header=\"esgf-idea-agent-type:basic_auth\" --http-user=\"$username_c\" --http-password=\"$password_c\"  $wget_args ${quiet:+-q} ${quiet:--v} -O $filename https://$orp_service/esg-orp/j_spring_openid_security_check.htm "


  #Debug message.
  if  ((debug))
  then
   echo -e "Executing:\n"
   echo -e "$command\n"
  fi 

  #Execution of command.
  http_resp=$(eval $command  2>&1)
  cmd_exit_status="$?"
  
  
  if ((debug))
  then
   echo -e "\nHTTP response:\n $http_resp\n"
  fi 

  #Evaluate response.
  #redirects=$(echo "$http_resp" | egrep -c ' 302 ')
  #(( "$redirects" != 7 )) ||
  if   echo "$http_resp" | grep -q "text/html"  ||  (( $cmd_exit_status != 0 ))   
  then
   rm "$filename"
   download_http_sec_retry     
  fi #if error during http basic authentication. 
  
}


download() {
    wget="wget ${insecure:+--no-check-certificate} ${quiet:+-q} ${quiet:--v} -c ${force_TLSv1:+--secure-protocol=TLSv1} $PKI_WGET_OPTS"
    
    while read line
    do
        # read csv here document into proper variables
        eval $(awk -F "' '" '{$0=substr($0,2,length($0)-2); $3=tolower($3); print "file=\""$1"\";url=\""$2"\";chksum_type=\""$3"\";chksum=\""$4"\""}' <(echo $line) )

        #Process the file
        echo -n "$file ..."

        #get the cached entry if any.
        cached="$(grep -e "^$file" "$CACHE_FILE")"
        
        #if we have the cache entry but no file, clean it.
        if [[ ! -f $file && "$cached" ]]; then
            #the file was removed, clean the cache
            remove_from_cache "$file"
            unset cached
        fi
        
        #check it wasn't modified
        if [[ -n "$cached" && "$(get_mod_time_ $file)" == $(echo "$cached" | cut -d ' ' -f2) ]]; then
                    if [[ "$chksum" == "$(echo "$cached" | cut -d ' ' -f3)" ]]; then
                echo "Already downloaded and verified"
                continue
            elif ((update_files)); then
                #user want's to overwrite newer files
                rm $file
                remove_from_cache "$file"
                unset cached
            else
                #file on server is different from what we have. 
                echo "WARNING: The remote file was changed (probably a new version is available). Use -U to Update/overwrite"
                continue
            fi
        fi
        unset chksum_err_value chksum_err_count
        
        while : ; do
            # (if we had the file size, we could check before trying to complete)
            echo "Downloading"
            [[ ! -d "$(dirname "$file")" ]] && mkdir -p "$(dirname "$file")"
            if ((dry_run)); then
                #all important info was already displayed, if in dry_run mode just abort
                #No status will be stored
                break
            else
                if ((use_http_sec))
                then
                 download_http_sec
                 if ((failed))
                 then
                  break
                 fi
                else
                 $wget -O "$file" $url || { failed=1; break; }  
                fi                
            fi

            #check if file is there
            if [[ -f $file ]]; then
                ((debug)) && echo file found
                if [[ ! "$chksum" ]]; then
                    echo "Checksum not provided, can't verify file integrity"
                    break
                fi
                result_chksum=$(check_chksum "$file" $chksum_type $chksum)
                if [[ "$result_chksum" != "$chksum" ]]; then
                    echo "  $chksum_type failed!"
                    if ((clean_work)); then
                        if !((chksum_err_count)); then
                                chksum_err_value=$result_chksum
                                chksum_err_count=2
                            elif ((checksum_err_count--)); then
                                if [[ "$result_chksum" != "$chksum_err_value" ]]; then
                                    #this is a real transmission problem
                                    chksum_err_value=$result_chksum
                                    chksum_err_count=2
                                fi
                            else
                                #ok if here we keep getting the same "different" checksum
                                echo "The file returns always a different checksum!"
                                echo "Contact the data owner to verify what is happening."
                                echo
                                sleep 1
                                break
                            fi
                        
                            rm $file
                            #try again
                            echo -n "  re-trying..."
                            continue
                    else
                            echo "  don't use -p or remove manually."
                    fi
                else
                    echo "  $chksum_type ok. done!"
                    echo "$file" $(get_mod_time_ "$file") $chksum >> $CACHE_FILE
                fi
            fi
            #done!
            break
        done
        
        if ((failed)); then
            echo "download failed"
            # most common failure is certificate expiration, so check this
            #if we have the pasword we can retrigger download
            ((!skip_security)) && [[ "$pass" ]] && check_cert
            unset failed
        fi
        
done <<<"$download_files"

}

dedup_cache_() {
    local file=${1:-${CACHE_FILE}}
    ((debug)) && echo "dedup'ing cache ${file} ..."
    local tmp=$(LC_ALL='C' sort  -r -k1,2 $file | awk '!($1 in a) {a[$1];print $0}' | sort -k2,2)
    ((DEBUG)) && echo "$tmp"
    echo "$tmp" > $file
    ((debug)) && echo "(cache dedup'ed)"
}

http_basic_auth_func_info_message()
{
  echo  "********************************************************************************"
  echo  "*                                                                              *"
  echo  "* Note that new functionality to allow authentication without the need for     *"
  echo  "* certificates is available with this version of the wget script.  To enable,  *"
  echo  "* use the \"-H\" option and enter your OpenID and password when prompted:        *"
  echo  "*                                                                              *"
  echo  "* $ "$(basename "$0")" -H [options...]                                     *"
  echo  "*                                                                              *"
  echo  "* For a full description of the available options use the help option:         *"
  echo  "*                                                                              *"
  echo  "* $ "$(basename "$0")" -h                                                  *"
  echo  "*                                                                              *"
  echo  "********************************************************************************"
}

#
# MAIN
#

if ((!use_http_sec))
then 
 http_basic_auth_func_info_message
fi

echo "Running $(basename $0) version: $version"
((verbose)) && echo "we use other tools in here, don't try to user their proposed 'options' directly"
echo "Use $(basename $0) -h for help."$'\n'

((debug)) && cat<<EOF
** Debug info **
ESG_HOME=$ESG_HOME
ESG_CREDENTIALS=$ESG_CREDENTIALS
ESG_CERT_DIR=$ESG_CERT_DIR
** -- ** -- ** -- ** --

EOF


cat <<'EOF-MESSAGE'
Script created for 53 file(s)
(The count won't match if you manually edit this file!)



EOF-MESSAGE
sleep 1

check_os
((!skip_security)) && find_credentials

if ((use_http_sec))
then 
     
 if (( ! insecure))
 then 
  get_certificates
 fi

 #Cookies folder.
 COOKIES_FOLDER="$ESG_HOME/wget_cookies"
 
 if (( force ))
 then
  if [ -d $COOKIES_FOLDER ] 
  then
   rm -rf $COOKIES_FOLDER
  fi
 fi

 #Create cookies folder. 
 if [[ ! -d $COOKIES_FOLDER ]] 
 then
  mkdir $COOKIES_FOLDER
 fi
 
 if((! use_cookies_for_http_basic_auth_start))
 then

  #Read openid.
  if [[ ! -z "$openId" ]]
  then
   openid_c="$openId"
  elif ( (("$#" > 1)) || (("$#" == 1)) ) 
  then
   openid_c=$1
  else
   read -p    "Enter your openid : " openid_c
  fi
  
  
  #Read username.
  if [[ ! -z "$username_supplied" ]]
  then
   username_c="$username_supplied"
  elif (("$#" == 2))
  then
   username_c=$2
  elif [[ "$openid_c" == */openid/ || "$openid_c" == */openid ]]
  then
   read -p    "Enter username : " username_c
  fi
  
  #Read password.
  read -s -p "Enter password : " password_c
  echo -e "\n"

 fi #use cookies

fi #use_http_sec 


#do we have old results? Create the file if not
[ ! -f $CACHE_FILE ] && echo "#filename mtime checksum" > $CACHE_FILE && chmod 666 $CACHE_FILE

#clean the force parameter if here (at htis point we already have the certificate)
unset force

download

dedup_cache_


echo "done"
